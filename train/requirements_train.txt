# Environment server deps (already in root requirements)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0
requests>=2.25.0

# Training
torch>=2.1.0
transformers>=4.46.0
trl>=0.12.0
datasets>=2.18.0
peft>=0.12.0
accelerate>=0.28.0

# Unsloth (install separately — see INSTALL below)
# unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git

# Logging / inspection
rich>=13.0.0
pandas>=2.0.0
